name: Modern map route start selection

on:
  pull_request:
    paths:
      - modern_map.html
      - tests/world-map-core.test.mjs
      - .github/workflows/modern-map-route-start.yml

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test tests/world-map-core.test.mjs
